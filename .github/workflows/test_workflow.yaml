
name: test_workflow

on:
  push:
    branches: [ "test" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ["self-hosted", "macOS", "ARM64"]

    steps:
    - uses: actions/checkout@v3
    - name: Setup conda environment
    - run: conda activate triton_example

    - name: Run a one-line script 
      run: echo Hello, world!
